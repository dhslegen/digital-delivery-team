# 代码评审检查清单 · <项目名称>

> 版本：v1.0 · 作者：review-agent · 日期：<YYYY-MM-DD>
> 评审范围：`git diff <base-branch>...HEAD`

---

## 强制检查维度（7 项，每项必须有判定）

### 1. 接口契约一致性

| 检查项 | 级别 | 判定 | 文件:行号 | 说明 |
|-------|------|------|---------|------|
| endpoint 路径与 api-contract.yaml 一致 | must-fix | pass / fail / n/a | | |
| request 字段与契约一致（无新增、无缺失） | must-fix | pass / fail / n/a | | |
| response 字段与契约一致 | must-fix | pass / fail / n/a | | |
| 错误码与契约枚举一致 | must-fix | pass / fail / n/a | | |

### 2. 异常处理与错误码

| 检查项 | 级别 | 判定 | 文件:行号 | 说明 |
|-------|------|------|---------|------|
| 所有异常均被捕获，无裸 throw 落地 | must-fix | | | |
| 错误响应体符合 ErrorResponse schema | must-fix | | | |
| 未将内部堆栈暴露给调用方 | must-fix | | | |

### 3. 并发安全（锁、事务、幂等）

| 检查项 | 级别 | 判定 | 文件:行号 | 说明 |
|-------|------|------|---------|------|
| 跨表操作在事务边界内 | must-fix | | | |
| 高并发写操作有锁保护 | must-fix | | | |
| 幂等接口有幂等键校验 | should-fix | | | |

### 4. 输入校验与注入防御

| 检查项 | 级别 | 判定 | 文件:行号 | 说明 |
|-------|------|------|---------|------|
| 所有外部输入经 schema 校验 | must-fix | | | |
| SQL 操作使用参数化查询，无字符串拼接 | must-fix | | | |
| 无 XSS / CSRF 漏洞（前端） | must-fix | | | |

### 5. 敏感信息与日志脱敏

| 检查项 | 级别 | 判定 | 文件:行号 | 说明 |
|-------|------|------|---------|------|
| 日志不含明文密码 / Token / PII | must-fix | | | |
| 代码中无硬编码密钥 | must-fix | | | |
| 响应体不返回不必要的敏感字段 | should-fix | | | |

### 6. 可观测性（logging / metrics / tracing）

| 检查项 | 级别 | 判定 | 文件:行号 | 说明 |
|-------|------|------|---------|------|
| 关键路径日志含 requestId | should-fix | | | |
| 错误日志含足够上下文（无需猜测） | should-fix | | | |
| 慢操作有超时或告警埋点 | nice-to-have | | | |

### 7. 依赖安全（新增依赖）

| 检查项 | 级别 | 判定 | 文件:行号 | 说明 |
|-------|------|------|---------|------|
| 新增依赖版本无已知 CVE | must-fix | | | |
| 新增依赖有明确用途（无冗余引入） | should-fix | | | |

---

## 汇总

| 级别 | 数量 |
|------|------|
| must-fix（阻塞） | <N> |
| should-fix（警告） | <N> |
| nice-to-have（建议） | <N> |

> 若 must-fix > 0，报告末尾必须注明：**必须修复后重跑 /review**

---

## Self-Check

- [ ] 7 个维度均已评估（无跳过）
- [ ] 每条 must-fix 有文件 + 行号 + 修复建议
- [ ] 已跑 `git diff` 核对评审范围
